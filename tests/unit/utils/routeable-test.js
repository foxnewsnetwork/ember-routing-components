import routeable from 'dummy/utils/routeable';
import { module, test } from 'qunit';

module('Unit | Utility | routeable', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = routeable();
    assert.ok(result);
  });
});
