{{#routing/-route state (action checkRoute state) (action redirectRoute state) as |state actions|}}
  {{yield
    (hash
      isActive=true
      route=state.route
      redirect=state.redirect
    )
    (hash
      redirect=actions.redirect
    )
  }}
{{/routing/-route}}
