{{#call-fn2 state routeId checkActive as |isActive|}}
  {{yield
    (hash
      isActive=isActive
      route=(component 'routing/-route' state checkActive redirect)
    )
    (hash
      redirect=(component 'routing/-redirect' routeId redirect)
    )
  }}
{{/call-fn2}}